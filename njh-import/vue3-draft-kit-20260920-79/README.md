# Vue 3 draft kit — 2026-09-20 (79th)

This directory holds one encrypted archive (`vue3-draft-kit-20260920-79.7z`, 7z AES-256 with
encrypted headers) and its checksum. The password is the same as the njh-cli kits and is
delivered separately.

## Verify and extract

```bash
sha256sum -c sha256.txt        # or: shasum -a 256 -c sha256.txt
7z x -p<password> vue3-draft-kit-20260920-79.7z
```

Then read `vue3-carry-20260909-v2/README-사내-반입.md` — its first section "지금 바로 시작하기" is the
command order for a fresh start.

## Requires

- The njh-cli replacement kit **v1.6.10** or later from this channel (`v1.6.10/`).

## Rehearsal

The full chain (00-env → 01 → 02 → 03 → 04 → 05 → 06, then a fresh start with 0c and the chain again)
was run on the lab tree before packaging; result **PASS**.

## What changed

- **22단계 분류 이름표 18개를 전부 한글로 맞췄다.** 78차에 새로 들어간 두 분류와 76차의 한 분류가 **영어 문장으로** 찍혀 나갔다(`Restore measured group-label…`·`Repair generated XLSX filename…`·`Literal Vuex login dispatch…`). 이름표는 운영자가 화면에서 읽고 무엇이 바뀌는지 판단하는 문구다. 게이트가 이제 **문구를 박지 않고** "분류마다 이름표가 있고 그 안에 한글이 있는지" 만 본다 — 영어 이름표를 하나 되돌려 넣어 규칙이 실제로 잡는 것까지 확인했다.
- **엑셀 묶음(그룹) 머리글 내보내기가 "영영 거부" 되던 원인은 전환 코드가 아니라 핀이었다 — 패키징 중 발견.** 공급자 확인이 여덟 개 자산의 **파일 전체 해시**로 판정하는데, 그 핀이 ① **워크트리 줄끝**에 걸려 있었다(Git for Windows 기본 `core.autocrlf=true` 이면 CRLF 로 체크아웃된다 → 같은 부품인데 해시가 다르다) ② **22단계가 매번 다시 쓰는** 개발 서버 의존성 미리 묶기 블록까지 포함한 설정 전체를 재고 있었다(그 블록에는 "킷이 만든다·손으로 고치지 않는다" 라고 적혀 있다 — 화면이나 라이브러리가 하나 늘면 해시가 달라진다) ③ 기준 바이트가 **75차 이전 호환 부품**이어서 지금 킷이 내는 어떤 산출물과도 맞지 않았다. 세 가지가 겹쳐 묶음 머리글이 있는 화면은 항상 "사람 확인" 으로 떨어졌다. 이제 `src/` 아래 텍스트 자산은 **LF 로 고른 뒤** 재고, 설정은 **킷이 소유한 블록을 뺀 나머지**(해석·변환에 쓰이는 부분)만 잰다. 블록 **밖** 변경은 그대로 거부한다. 판정 등급 자체는 손대지 않았다 — 여전히 **진단용 REVIEW** 이고 자동 승인이 아니다.
- **같은 실수의 계열을 게이트가 막는다.** 78차에도 같은 모양(앞 분류가 고치는 파일의 전체 해시에 핀)이 실제로 발견됐다. 이번 게이트는 (a) 복원 도구 8개에 64자리 해시 리터럴이 **하나도 없는지**, (b) CRLF 체크아웃만으로 공급자 계약이 거부되지 않는지, (c) 킷 소유 블록이 자라도 거부되지 않는지, (d) 블록 밖 변경은 **여전히 거부되는지**(계약이 사라지지 않았는지) 를 모두 본다. 정규식이 빈손으로 통과하지 못하게 하는 반증 단언도 같이 넣었다.
- **격자 머리글 행의 실측 차이를 되돌렸다 — 22단계 `css-restore` 안의 새 규칙.** 새 표 라이브러리는 `.ag-header.ag-header-allow-overflow .ag-header-row { overflow: visible }` 를 넣는데, 고정 높이 36px 머리글에서는 이것이 머리글 글자 래스터화를 바꾼다. 행 선택자 하나만 격리해 차이를 재현했고(머리글 칸·라벨 컨테이너를 바꿔서는 설명되지 않았다), **측정한 자리에만** `overflow: hidden` 을 되돌린다. 전역 덮어쓰기가 아니다. **행이 있는 상태**로 열 가지 화면 상태(기본 8 + 행 선택 + 2페이지)를 다시 쟀다 — 이전에는 빈 격자만 재서 행 여백·줄무늬·선택 강조·페이지 번호를 아예 못 쟀다. 수치(킷 안 `analysis-reference/header-row-restore/applied-acceptance.json`): 닫힌 계산값 **44**(그 중 머리글 행렬 상태 02~06 이 8+8+8+8+4 = **36**), 그룹이 있는 탭 **네 곳이 각각 715픽셀** 복원, 남은 대상 차이 **0**, 새로 생긴 계산값·픽셀 차이 **0**, 복원 규칙 드리프트 **0**. 검증본에 복원 CSS 를 주입하지 않고 **이전 값만 되돌리는 대조**를 빼서 실제 적용된 스타일시트를 드러내는 방식으로 쟀다.
- **주 메뉴(상단 확장 영역)의 구조·글꼴·간격도 실측값으로 되돌린다.** 77차에 글꼴만 되돌리던 블록을 구조까지 포함한 한 블록으로 **올려 쓰고**(옛 블록은 지운다), 루트 식별자·확장 슬롯·동적 메뉴·활성화 버튼 계약이 **다 맞을 때만** 바꾼다. 하나라도 다르면 사유를 남기고 건드리지 않는다. 두 복원 도구 모두 **앞 분류가 같은 파일을 이미 고친 상태에서도** 계속 도는 것을 스스로 시험한다.
- **조건부 탭·차트 도구맵·생략된 정렬 기본값.** 옛 UI 라이브러리의 조건부 탭 묶음을 킷 탭 부품으로 바꾸고(모델·조건·클릭·비활성 속성만 있는 모양에서만), 차트 도구맵 기본값을 이전 판과 같게 주는 얇은 부품을 넣고, 이전 판에만 있던 그리드 **정렬 기본값**을 as-is 스냅샷에서 읽어 되살린다. 세 가지 모두 소유자가 불분명하거나 화면이 이미 정해 둔 경우에는 거부한다.
- **배정 33건을 전부 정리했다 — 다만 "33건 수정" 이 아니다.** 12건은 코드 수정, 나머지는 실측한 잔여값 보수·첫 화면 읽기 보수·소유자 관문 종료이고, **2건은 원래 동작**(전환 결함이 아니었다), **2건은 원래 차이 보고 자체가 무효**였다. 되돌릴 대상이 아닌 것을 되돌리지 않은 것도 결과로 적는다.
- **스캐너가 "못 본 것" 을 "깨끗하다" 로 찍지 않는다.** 검출기 진입점이 ① 따라가지 않은 심링크 ② 지원하지 않는 확장자 ③ UTF-8 로 못 읽는 입력·NUL 이 든 입력 ④ **볼 수 있는 소스가 0개** 인 경우를 각각 **진단으로 남긴다**(전에는 조용히 "결함 0" 이었다). 잔재·사용처 스캐너는 이제 JS 의 모든 줄끝(CR·LS·PS)에서 줄·칸을 바르게 세고, 이스케이프가 든 문자열과 치환 없는 템플릿 리터럴 `import()`·`require()` 를 읽고, `.cjs/.mts/.cts/.jsx/.tsx` 까지 본다. **해석할 수 없는 동적 가져오기**가 있으면 도달 범위 잘라내기를 끄고 경고로 남긴다 — 그대로 두면 아직 쓰는 파일을 "안 쓰임" 으로 볼 수 있었다.
- **그림 한 장이 "라이브러리 전환 완료" 판정을 무너뜨리던 결함을 잡았다 — 패키징 중 발견.** 사용처 스캐너에 이번 차수에 들어간 규칙 하나가 **소스 인벤토리 밖의 지역 의존**을 만나면 프로젝트 전체의 도달성 계산을 껐다. 그 규칙의 이유("건너뛴 파일이 소스로 되돌아오는 import 를 가질 수 있다")는 옳지만, 화면 하나가 `public/` 아래 **그림 파일**을 참조한 것까지 걸렸다 — 번들러가 URL 로만 바꾸는 무생물 자산에는 되돌아오는 간선이 없다. 도달성이 꺼지자 **73차의 킷 어댑터 면제도 함께 꺼졌고**(도달성 미확정이면 어댑터를 증거로 쓰지 않는다 — 의도한 보수적 판정이다), 그 결과 킷 어댑터가 일부러 유지하는 전역 이름(쿠키·알림)이 "아직 그 라이브러리를 쓰는 중" 으로 잡혀 남은 라이브러리가 2종 → 5종으로 보였다. 무생물 자산(그림·글꼴·미디어·PDF)은 도달성을 끄지 않게 좁히고, 코드일 수 있는 확장자와 확장자 없는 경로는 **그대로 끈다**. 되돌려 넣으면 붉어지는 회귀 시험과, 리허설에서 도달성 경고가 0 이고 어댑터가 실제로 인식되는지 보는 게이트 단언을 함께 넣었다. **두 기능이 한 변수로 묶여 있으면, 한쪽을 보수적으로 만드는 변경이 다른 쪽을 조용히 끈다** — 이 결합은 그대로 남아 있으므로 도달성을 끄는 조건은 앞으로도 좁게 유지해야 한다.
- **사진용 보고(23단계)가 만들다 멈춘 절을 감추지 않는다.** 절을 만들다 예외가 나면 그 사실을 한 줄로 찍고, 화면 한 장에 들어갈 줄이 모자라면 **불확실 경고(※)를 먼저** 배치한다. 사유 목록이 비었는데 검토·일부 집계가 0 이 아니면 "미상" 이라고 적는다.
- **빈 화면 판정을 한 계약으로 모았다.** 0바이트·공백/주석만·속성 없는 빈 템플릿 세 모양을 구분하는 읽기 계약 파일이 킷 안 **7곳**에 같은 내용으로 실려 있고, 게이트가 사본이 어긋나지 않았는지 본다(어긋나면 읽는 쪽마다 다르게 판단한다). 읽기는 **합성 템플릿을 만들지 않고 구문 오류를 감추지 않는다**. 원본이 비어 있지 않았는데 결과가 비었으면 면제가 아니라 **내용 유실**이고, 기준본이 없으면 면제가 아니다.
- **엑셀 — 묶음 머리글 전용 실행 부품과 열 투영·자른 값 프로필.** 묶음 머리글 내보내기용 실행 부품을 갈라내고, 일반 표 내보내기에 **열 투영**(키를 뒤에 덧붙이지 않는 명시적 배열 투영)과 **자른 값** 프로필을 명시적 스위치로만 켠다 — 스위치 없이는 거부한다. 열 16,384·칸 500,000 상한을 넘으면 거부한다.
- **결과로 나타난 수치 — 엑셀 축 자동 전환이 랩 분석 사본에서 10 → 14 화면으로 늘고, 사람 확인 대기가 34 → 30 으로 줄었다.** 라이브러리 대조 결과가 같은 화면은 14/14 다. 같은 사슬 리허설에서 78차는 자동 10·검토 34 였다 — 위의 핀 수정과 새 인식기가 실제로 네 화면을 사람 손에서 떼어 냈다는 뜻이다.
- **킷 도구 테스트 전수 1,050건 통과 · 실패 0 · 건너뜀 0**(115개 파일 — 도구·검출기·동작 비교 계약·1차 lint). 78차의 605건에서 늘었다.
- **증명하지 못한 것(감추지 않는다).** ① 묶음 머리글 공급자 판정은 여전히 **진단용 REVIEW** 다 — 묶음 내보내기가 자동 승인되지 않으며, 번들러 모듈 해석·실행 시점 수치 범위·비동기 호출자 계약·전환된 브라우저 조작은 미해결로 남아 있다. ② 정렬 측정은 설치된 브라우저 한 종·창 크기 한 가지, 행이 있는 열 가지 상태에서 쟀다 — 반응형 폭·다른 권한 상태는 재지 않았다. 대시보드 카드의 **높이 차이**는 이번에도 손대지 않았다. ③ 빈 화면 읽기 계약은 **아직 모든 읽는 쪽에 들어가지 않았다** — 검출기 설치본 단위 해석과 1차 lint 면제는 별도로 남아 있고, 계약 파일이 있다는 사실만으로 파서 일관성을 단정할 수 없다. ④ 잔재 위치 보고는 혼합 화면의 비-LF 좌표에서 아직 어긋날 수 있다. ⑤ 킷에 함께 실리는 스킬 연구 묶음 테스트(491개 파일) 가운데 **97개 파일**은 랩 전용 입력(특정 기준 커밋·오프라인 설치본·브라우저)이 있어야 돌아 이번 환경에서는 돌지 않았다 — 78차와 같은 상태이고 **이번 변경으로 새로 깨진 파일은 0개**다(같은 환경에서 19개 파일이 오히려 통과로 돌아섰다). ⑥ 내장 검출기 자산 사본 하나(`bundled-skills/…/detector-kit/tools/excel-dataflow.mjs`)가 본체와 어긋나 있다 — 78차 이전부터 그랬고 이번에 맞추지 않았다(자산 핀·출처 기록을 같은 변경에서 옮겨야 해서 별 차수로 남긴다).
