# njh-import v1.5.321 — 정본 (v1.5.316 을 대체합니다)

| 파일 | 대상 | 필수 | 크기 |
|---|---|---|---|
| `njh-cli-v1.5.321.7z` | 개인 PC / 노트북 njh 본체 | **필수** | 18.5 MiB |
| `njh-win-laptop-v1.5.321.7z` | 노트북 로컬 LLM 도구 | **필수** | 1.65 MiB |
| `njh-gateway-v1.5.321.7z` | AP03 게이트웨이 | 게이트웨이 담당자만 | 1.88 MiB |

압축 암호는 **별도 채널**로 전달됩니다. 이 저장소에는 어떤 형태로도 들어있지 않습니다.

> **⛔ `njh-import/v1.5.316/` 은 쓰지 마십시오.** 그 킷은 빌드 매니페스트가 없어 검증이 불가능했고
> 착지하지 않은 브랜치의 문서를 담고 있었습니다. 이 v1.5.321 이 그 두 결함을 모두 닫은 정본입니다.

## 이 킷이 v1.5.316 과 다른 점 — 받는 쪽에서 **검증할 수 있습니다**

| | v1.5.316 (비정본) | **v1.5.321 (정본)** |
|---|---|---|
| 빌드 매니페스트 | ❌ 없음 | ✅ `c5-build.json` · `gateway-build.json` · `laptop-build.json` |
| 소스 커밋 증명 | ❌ 없음 | ✅ `source-commit-attestation.json` |
| 안전 게이트 결과 | 동봉 안 됨 | ✅ `safety-gate.json` (3종 전부 `pass`, 누출 **0건**) |
| 빌드 근거 | 없음 | ✅ `package-build-evidence.md` |
| 소스 | 미착지 브랜치 | ✅ staging 정본 `ddd1cad593cf` |
| C5 항목 수 | 309 (검증 불가) | **312** (매니페스트로 대조 가능) |

`sha256.txt` 만으로는 "받은 파일이 보낸 파일과 같은가"만 알 수 있습니다.
**이 킷은 거기에 더해 "그 파일이 어느 소스에서 나왔고 무엇이 들어 있는가"를 함께 줍니다.**

## 타겟점검 검토보고가 들어 있습니다

CLI 킷 안 **`docs/타겟점검-AI-고도화-검토보고.html`** — 압축을 풀고 브라우저로 열면 됩니다.
외부 CDN·폰트·이미지 참조가 0이라 폐쇄망에서도 그대로 열립니다.

**문서 최상단에 「사업부서 요청 — 그래서 어떻게 처리하는가」 요약 절이 있습니다.** 목차 첫 항목이기도 하고,
그 아래는 전부 그 요약의 근거입니다. 바쁘면 최상단 절만 읽으면 됩니다. 요약이 답하는 것:

- 사업부서 안과 요청자 안이 각각 **어디까지 맞고 어디서 틀리는가**
- LLM 의 승부처가 `실판매자` 하나로 뚜렷한 이유 — 명의상 판매자(`sale_user_id`)와 실제로 판 사람이 다르고,
  후자는 **필드가 아니라 본문 텍스트에만** 있다
- 일 1만 건 파일럿의 처리 흐름과 **실측 소요**(본문 1,000자 기준 3.19시간 → 야간 배치 안)
- 우리가 **지는 계약과 지지 않는 것**의 경계
- 지금 막혀 있어 **사업부서에 요청해야 할 5가지**

## 0. 받은 파일 확인 (풀기 전)

```bash
shasum -a 256 njh-cli-v1.5.321.7z njh-gateway-v1.5.321.7z njh-win-laptop-v1.5.321.7z
```

`SHA256SUMS.txt` 와 **한 글자라도 다르면 풀지 말고 다시 받으십시오.**

## 1. 설치

```bash
# CLI 킷 (모든 사람)
7z x njh-cli-v1.5.321.7z -o"$HOME/njh-cli-v1.5.321"
cd "$HOME/njh-cli-v1.5.321"
bash setup.sh
"$HOME/.njh-cli/bin/njh" --version     # → 1.5.321 이 찍혀야 합니다

# 타겟점검 검토보고 열기
open docs/타겟점검-AI-고도화-검토보고.html      # Windows 는 start, 또는 브라우저로 드래그

# 노트북 로컬 LLM 도구
cd /c/njh-local-llm
./verify-local-llm.sh      # ← 먼저 이것부터
./start-local-llm.sh
njh
ollama.exe ps              # PROCESSOR 가 GPU 로 유지되면 끝

# 게이트웨이 담당자만
njh onboard gateway && bash "$HOME/.njh-cli/scripts/njh-connect-doctor.sh"
```

⚠️ **예전 버전 폴더의 `njh` 는 쓰지 마세요.** PATH 도 새 위치를 보게 하십시오.
버전이 섞이면 로컬 LLM 이 적재 옵션 불일치로 모델을 통째로 재적재하고, 그 재적재가 GPU 가 아니라 CPU 로 착지합니다.

## 2. 검증된 것

| 항목 | 결과 |
|---|---|
| 안전 게이트 3종 | ✅ 전부 `pass` |
| 누출 스캔 | ✅ **0건** (cli · gateway · win-laptop) |
| SHA-256 재계산 | ✅ `sha256.txt` 와 일치 |
| 소스 커밋 | `ddd1cad593cf` — origin 에 존재 확인 |
| C5 / Gateway / Laptop 항목 수 | 312 / 37 / 18 |
| 타겟점검 문서 동봉 | ✅ 매니페스트에서 확인 |

🚨 **아직 main 은 아닙니다.** origin/main 은 v1.5.312 이고 이 킷은 staging 정본에서 나왔습니다.
취합 게이트가 아직 돌고 있어서이며, **런타임 동작에는 영향이 없습니다.**

## 3. 증명되지 않은 것 — 사내망에서만 확인 가능

인증된 Codi 호출, 사내 SQMS 조회, 게이트웨이 3대 함대 수용 시험은 **여기서 관측된 적이 없습니다.**
붙지 않으면 그것은 `BLOCKED_INTERNAL_NETWORK` 이지 PASS 가 아닙니다. 그렇게 기록해 주십시오.

## 4. 막혔을 때 볼 곳 — 전부 받은 패키지 안에 있습니다

| 상황 | 문서 |
|---|---|
| 설치 직후 30분 | `docs/사용자/06-post-import-first-30-minutes.md` |
| 접속·TLS 실패 | `docs/사용자/04-문제-해결.md` |
| 게이트웨이 연결 | `docs/사용자/게이트-연결-테스트.md` |
| 처음 쓰는 사람 | `docs/사용자/01-빠른-시작.md` |

TLS 한 줄 요약: 사내 CA 문제는 **검증을 끄지 말고 `caBundle` 로** 해결합니다.
기본 설정이 이미 사내 CA 추출을 시도하므로 **첫 실행의 TLS 경고는 정상**이며 실패가 아닙니다.

---

빌드 2026-08-11 · 소스 `ddd1cad593cf` · 누출 0건 · 아카이브는 `-mhe=on` 으로 헤더까지 암호화됩니다.
