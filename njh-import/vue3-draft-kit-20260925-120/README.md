# Vue 3 draft kit — 2026-09-25 (120th)

This directory holds one encrypted archive (`vue3-draft-kit-20260925-120.7z`, 7z AES-256 with
encrypted headers) and its checksum. The password is the same as the njh-cli kits and is
delivered separately.

## Verify and extract

```bash
sha256sum -c sha256.txt        # or: shasum -a 256 -c sha256.txt
7z x -p<password> vue3-draft-kit-20260925-120.7z
```

Then read `vue3-carry-20260909-v2/README-사내-반입.md` — its first section "지금 바로 시작하기" is the
command order for a fresh start.

## Requires

- The njh-cli replacement kit **v1.6.10** or later from this channel (`v1.6.10/`).

## Rehearsal

The full chain (00-env → 01 → 02 → 03 → 04 → 05 → 06, then a fresh start with 0c and the chain again)
was run on the lab tree before packaging; result **PASS**.

## What changed

- 120차는 사내 119차 결과 사진 8장 판독 — ③ 동작 동등성 범위를 막던 판정 결함과, 모든 화면의 알림창이 as-is 와 다르게 보이던 것의 수리다. 초안 규칙은 119차와 같다(12 --kit 다시 안 돌림)
- 17 권한 밖 화면이 면제되지 못하던 것: as-is 라우터 가드는 권한이 없을 때 이동을 지정한 뒤에도 아래로 내려가 next 를 한 번 더 부른다 — Vue Router 4 는 개발 서버에서 "next 두 번" 경고를 내고, 운영 빌드로 뜨는 as-is 의 Vue Router 3 은 조용하다. 119차는 Vue 개발 경고만 비교에서 뺐다 → 그 문구와 이동 중 예외를 한 번 더 알리는 두 문구만 뺀다(예외 자체는 오류 모양으로 따로 비교, 빠진 경로 같은 다른 Vue Router 경고는 그대로). 랩 권한 경로 프로브로 재현
- 17 조회 뒤 to-be 에서만 AG Grid 속성 이름 검사 경고가 나 성공 제외되던 것: AG Grid 25 는 격자를 처음 만들 때 최상위 열 정의만 한 번, 31 은 열 정의를 바꿀 때마다(묶음 안까지) 이름을 검사한다. 17 이 as-is 설치본의 속성 목록(ColDefUtil·PropertyKeys)을 읽어, 이름이 25 의 열·격자 목록 어디에도 없고 as-is 소스에도 그 키가 있을 때만(같은 오타) 판 진단으로 빼고 보고서에 따로 둔다. as-is 쪽 경고는 같은 화면에서 to-be 가 증명한 이름만. 25/31 안내 줄 주소 차이도 같은 정체로
- 17 화면을 열자마자 뜬 알림에 조회 버튼이 가려 "누를 조회 없음" 이던 것: 버튼이 확인·닫기 하나뿐이고 입력·격자가 없는 알림만 닫고 그 뒤의 조회를 누른다. 알림 글자는 지문으로만 남겨 두 쪽 비교(화면 열기·누르기 전·버튼 진단), 확인 클릭이 만든 요청·예외·경고도 그 조작 기록으로. 확인/취소 대화상자·세션 만료·로그인·쓰기 문구 알림은 건드리지 않고 쓰기 요청은 그대로 막힌다. 닫다 다른 화면으로 가면 면제 안 함. 랩: 알림에 가렸던 화면 6개가 짝지은 조작 같음 8 로
- 08b·17 팝업 화면 판정 비대칭: 두 쪽 모두 부모 창이 없어 같은 예외로 멈추는데 Vue Router 3 은 첫 이동 실패 뒤 주소를 로그인으로 되돌려 as-is 만 "로그인으로 튕김" 이었다. 이때 라우터가 확정한 경로가 없다(matched 0 — 랩 두 쪽 실측, 가드 이동은 1 이상) — matched 0 + 예외 + 팝업 신호면 도착 주소와 무관하게 "부모 데이터 필요", 팝업 면제도 matched 수가 같아야
- 22 z-scale 라이브러리 겹침 값: 102차 척도가 앱 CSS 만 옮겨 전체 화면 덮개(10 → 1110)가 알림창(1060) 위로 올라가, 덮개가 있는 화면에서 알림 확인 버튼을 누를 수 없었다(랩 elementFromPoint 로 확인). 두 설치본의 알림창·진행 막대 겹침 값을 같은 단조 사상으로 옮긴 생성 시트를 main 에서 한 번(as-is 순서 덮개 < 알림창 < 로딩 덮개 유지)
- 22 z-scale 알림창 모양: 킷의 sweetalert2 9 → 11 상향으로 여백 구조·버튼 좌우 여백·글자 크기·기본 확인 색이 달라 모든 알림창이 as-is 와 달랐다(확인 버튼 99×46 → 65×44). as-is 9·10 · to-be 11 이상일 때만 모양 호환 시트를 main 에서 한 번 — 랩 알림·확인/취소·오류·글만 네 모양 요소 위치·크기 같음, 알림 한 장 픽셀 차이 0
- 23 "잔재 보고서가 없다": 23 이 찍기 전에 잔재 보고서를 새로 만든다(예전엔 13 만 만들었다), 09 도 src 전체를 보면 갱신
- 23 사진 [4/5]: 두 쪽 같은 모양인데 면제하지 못한 화면을 막은 이유별 화면 수로(화면마다 다른 경로 글자는 접는다) + 비교에서 뺀 진단 수 한 줄 — 다음 사진 한 장으로 원인을 가른다
- 17 누르기 전 대기: 열자마자 탭 여러 개를 차례로 조회하는 화면은 느린 WAS 에서 탭 조작 창에 그 연쇄 조회가 섞여 요청 본문 값·요청 수가 두 쪽에서 다르게 잡혔다(사내 119차 요청 다름). 대기 중 요청이 0.6초 없을 때까지(최대 15초) 기다린 뒤 누르고, 끝내 잠잠해지지 않은 조작은 성공 짝에서 뺀다
- 적대 검토(opus) 반례 7건 재현·수리: 경고 접두어째 빼기·다른 자리로 옮긴 속성·다른 화면의 증명·알림 뒤 이동·알림 확인 효과·가드 이동 뒤 예외·--limit 부분 기록
- 진단용 부분 스윕(ROUTE_SWEEP_ONLY)·--limit: 17 비교는 그 영수증을 무효로 본다
- 사내 순서: 22 z-scale → 08a → 17(두 쪽 다시) → 09 → 23 → 00b → 24
- 랩 17 끝-대-끝(as-is 기동부터 비교까지): 짝지은 조작 52 → 59·같음 59·차이 0, 비교 못 한 화면 15 → 5(두 쪽 같은 예외로 실패한 조회 4·초안 가드로 to-be 만 뜨는 화면 1), 비교 불가(양쪽 같은 모양) 31 → 35, 조작 68개 모두 누르기 전 잠잠함 확인
- 테스트: 17 판정·알림 판정·튕긴 팝업/알림 효과/누르기 전 대기 실브라우저 3·라이브러리 겹침/알림창 모양 끝-대-끝 2·사진 1 추가, revert-red 확인. 게이트 120 블록(정적 확인·23 잔재 보고서 실행·120 테스트 13)
