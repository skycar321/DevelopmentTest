# Vue 3 draft kit — 2026-09-20 (78th)

This directory holds one encrypted archive (`vue3-draft-kit-20260920-78.7z`, 7z AES-256 with
encrypted headers) and its checksum. The password is the same as the njh-cli kits and is
delivered separately.

## Verify and extract

```bash
sha256sum -c sha256.txt        # or: shasum -a 256 -c sha256.txt
7z x -p<password> vue3-draft-kit-20260920-78.7z
```

Then read `vue3-carry-20260909-v2/README-사내-반입.md` — its first section "지금 바로 시작하기" is the
command order for a fresh start.

## Requires

- The njh-cli replacement kit **v1.6.10** or later from this channel (`v1.6.10/`).

## Rehearsal

The full chain (00-env → 01 → 02 → 03 → 04 → 05 → 06, then a fresh start with 0c and the chain again)
was run on the lab tree before packaging; result **PASS**.

## What changed

- **눈에 다르게 보이던 정렬 세 곳을 원인까지 규명해 되돌렸다 — 22단계 새 분류 `layout-restore`.** 처음 세운 UI 가설 두 개는 **둘 다 틀렸다**. ① 표 라이브러리의 **그룹 머리글**: 가운데 정렬 선언은 양쪽 트리에 **이미 있었고**(앱 CSS 가 직접 준다) 하나 더 넣어도 고쳐지지 않는다. 실제 원인은 새 판이 라벨에 붙이는 sticky 규칙(`flex: none`)이라 **라벨이 글자 폭으로 줄어든** 것이었다(이전 판은 `1 1 auto`). 바깥 칸은 차이의 원인이 아니다. ② **대시보드 카드의 합계**: `float` 은 양쪽 다 살아 있고 버튼 내용은 양쪽 다 flex 다. 실제 원인은 목표 UI 라이브러리가 이전 판의 `.v-btn__content { flex: 1 0 auto }` 를 **없앤** 것이라 내용이 줄어들어 이미 있던 `width:100%` 가 한 줄을 다 못 쓴 것이었다. 두 경우 모두 **브라우저에서 그 선언 하나만 되돌리는 개입으로 격리**했고, 개입을 빼면 결함이 다시 나오는 것까지 확인한 뒤 킷을 고쳤다.
- **측정치(랩, 설치된 브라우저·실측 계산값).** 그룹 머리글 라벨 폭(이전 판 / 전환 후 / 되돌린 뒤): 668 / 67.92 / 668 · 149 / 32.56 / 149 · 322 / 57.19 / 322 · 145 / 0 / 145. 되돌린 뒤 **그룹 라벨·글자 16자리의 사각형이 이전 판과 일치**했고, 아래 단 머리글의 구조·계산값·사각형은 **전·후가 같았다**(건드리지 않았다). 대시보드: 카드 내용 폭 189.73 → 339(이전 판 339), 합계 오른쪽 끝 1305.36 → 1380(이전 판 1380). 그룹이 있는 탭은 렌더된 본문 행 20개·그룹 칸 4개(빈 마지막 그룹 포함)로, **그룹 머리글이 실제로 있는 상태에서 쟀다** — 그룹이 없는 화면만 재고 "같다" 고 했던 이전 판정은 근거가 아니었다.
- **측정한 자리에만, 재지 않은 화면은 건드리지 않는다.** 전환 조건: 설치본에 그 sticky 규칙(또는 없어진 내용 flex 선언)이 실제로 있어야 하고, 화면에 실제 표 부품과 **가운데 정렬 그룹 정의**(자식이 비지 않은 것)가 있어야 하고, 대시보드는 이미지·이름·오른쪽 정렬 합계로 된 **측정한 전폭 버튼 구조**여야 한다. 화면 스타일이 **이미 그 자리를 정해 두었으면 손대지 않는다.** 위치로 찍는 선택자 대신 그 자리에 표지 속성 **한 개**만 붙이고, `!important`·레이어 조작·전역 규칙은 쓰지 않았다. 두 번 돌리면 아무것도 바꾸지 않는다(멱등).
- **앱 껍데기를 "파일 전체 해시" 로 알아보던 방식은 폐기했다(패키징 중 발견).** 같은 22단계 **안에서** 앞 분류가 그 파일에 스타일을 넣고 뒤 분류가 또 바꾸므로, 해시 고정은 **새 트리에서 어떤 상태와도 맞지 않아** 그 규칙이 늘 조용히 거부됐다. 측정이 기댄 것은 바이트가 아니라 **레이아웃 층 구성**(층의 종류·순서·개수가 위쪽 층의 z-index 를 정한다)이므로 그것을 직접 본다. 앞 분류가 넣은 스타일에는 흔들리지 않고, 층이 실제로 바뀌면 거부하는 회귀 시험을 함께 넣었다.
- **엑셀 공유 부품이 거부된 구형 엔진을 끌고 오던 경로를 끊었다.** 호출자 전용 내보내기·올리기 부품을 갈라내 공유 부품이 스프레드시트 엔진을 **직접 가져오지 않게** 했고, 생성되는 실행 코드 **31개 파일이 1차 lint 를 제외 하나 없이** 통과한다(파일명 검사가 제어문자 규칙을 깨뜨리던 것은 제외가 아니라 **생성기**에서 고쳤다 — 22단계 새 분류 `excel-filename`). 랩 브라우저에서 내려받기·올리기 동등성을 확인했다.
- **검출기·보고서가 입력에 없는 것을 단정하지 않는다.** ① 사용처 스캐너가 **설정된 경로 별칭 뒤의 import** 를 "안 쓰임" 으로 보던 거짓 음성을 고쳤다 — 그대로 두면 **아직 쓰는 라이브러리를 지울 수 있었다**(별칭·정규식·재내보내기·동적 가져오기까지 정적으로 해석하고, 해석이 안 되면 잘라내기를 끄고 불확실로 남긴다). ② 사진용 보고(23단계)는 이전 판 비교본을 **덜 쟀으면** "결함 0건" 으로 확정하지 않고 **판단 불가**로 남기고, 비교 범위가 미완료임을 먼저 찍는다. 이전 판이 뜨는데 전환 후가 실패한 것만 "전환", 양쪽 실패는 "원래 그런 것", 비교본이 없는 것은 "판단 불가" 다. ③ **입력과 무관하게 과거 측정 결과를 반복하던 문장을 없앴다** — 지금 입력에 실패가 있는데도 "실패 0" 을 주장할 수 있었다(전수 측정 사실은 근거가 있는 산출물에 그대로 남는다). ④ 1차 lint 의 "빈 화면 면제" 가 **이전 판도 빈 화면일 때만** 적용된다 — 내용이 있던 화면을 비워도 조용히 넘어가던 구멍을 닫았다.
- **전환만 하고 선언은 남던 구멍을 21b 단계로 닫았다.** 사용처가 **0 인 선언만** 지우고(잠금 파일까지), 빌드·화면 렌더·재실행 무변경을 증거로 남긴다. 하나라도 쓰는 곳이 있으면 **그 이름은 지우지 않고 현재 수치와 함께 남긴다** — 엑셀 계열은 아직 쓰는 화면이 있어 그대로 둔다. 실제로 지운 것: 사용처 0 인 옛 스타일 패키지 1개와 아무도 부르지 않는 파일 2개. 나머지 36개 선언은 **선언·설치 상태가 서로 맞는지** 확인만 했다.
- **읽기 전용 상호작용 비교(17단계)가 성립하지 않은 짝을 세지 않는다.** 양쪽 클릭이 다 실패했거나 상태를 못 읽은 짝을 "짝지었다" 로 세던 것을 막고, 비교 완전성을 요구하는 스위치를 넣었다.
- **게이트 보강 — 이번 차수에 게이트 자체가 네 번 막혔고 그 원인을 고쳤다.** ① 테스트 **개수 고정 핀**은 레인이 시험을 **늘리면** 깨진다(즉 킷이 좋아질 때만 죽는다) — 열세 자리를 전부 "그만큼은 돌았다 · 실패 0 · 건너뜀 0" 으로 바꿨다. ② 그 판정은 `grep … | wc -l` 이 아니라 `awk` 로 한다(0건일 때 종료코드가 1 이라 합격할 때 죽는다). ③ 새 단언은 **개수를 박지 않는다**(있는지 · 0 인지 · 순서가 맞는지). ④ 계약을 **의도적으로 바꾼 자리**(사진용 보고의 고정 문장 제거)는 같은 변경에서 게이트 단언도 함께 고쳤다. 새로 게이트가 확인하는 것: 새 분류 두 개가 등록·전환·멱등한지, 넣은 스타일이 실측 flex 값 그대로이고 `!important`·레이어가 없는지, 앱 껍데기 가드에 파일 전체 해시가 **돌아오지 않았는지**, 공유 엑셀 부품이 엔진을 끌고 오지 않는지, 사진용 보고가 **네 가지 입력 상태**에서 입력대로만 결론을 내는지, 21b 가 반입 순서·재개 순서에 배선됐는지, 반입 절의 명령 순서가 계약과 같은지.
- 킷 도구 테스트 전수 **605건 통과 · 실패 0 · 건너뜀 0**(81개 파일). 77차까지의 변경 포함. 테스트가 **킷 node_modules 연결 없이** 돌게 맞췄다(연결을 못 만드는 환경이 실제로 있었다).
- **증명하지 못한 것(감추지 않는다).** ① 엑셀 축 자동 전환은 **검토 대상 31화면**이 남았고 묶음 내보내기 계열은 별도 생성기가 필요하다 — 엑셀 계열 라이브러리를 지우는 것은 그 뒤다. ② 읽기 전용 상호작용 비교는 라우터 77화면 중 **57화면**까지만 짝이 성립했고(남은 차이 2묶음·차트 도움말 5건 미해결) **전체 동등성은 확정되지 않았다**. ③ 대시보드 카드의 **높이 차이**(내용 높이·라벨 높이)는 이번에 손대지 않았다 — 원인이 줄 높이라 고정값으로 넣지 않았다. ④ 전체 화면 층 복원은 **랩에서 측정한 층 구성**을 기준으로 하므로, 층 구성이 다른 앱에서는 사유와 함께 사람 확인으로 남는다. ⑤ 정렬 측정은 설치된 브라우저 한 종·창 크기 한 가지에서 쟀다 — 반응형 폭·다른 권한 상태·0 이 아닌 실제 합계는 재지 않았다.
