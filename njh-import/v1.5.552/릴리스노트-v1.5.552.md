# njh-cli 교체 키트 릴리스 노트 — v1.5.552 (2026-09-03)
 <!-- test-derived-document-contract:start; generated from executable test assertions; LOAD-BEARING document lines=1@scripts/user-guide-sync.test.ts:2889,3@scripts/user-guide-sync.test.ts:2890,13@scripts/user-guide-sync.test.ts:2891,13@scripts/user-guide-sync.test.ts:2895,15@scripts/user-guide-sync.test.ts:2903,16@scripts/user-guide-sync.test.ts:2906,17@scripts/user-guide-sync.test.ts:2907,21@scripts/user-guide-sync.test.ts:2908,31@scripts/user-guide-sync.test.ts:2910; run npx tsx scripts/recipient-document-contract.ts --write after changing this document or its test; test-derived-document-contract:end -->
이 문서는 `v1.5.552` 교체 키트와 함께 전달됩니다. v1.5.551 직후의 통합판이며 **실행 파일 변경이 없습니다**(클라이언트·게이트웨이 번들은 v1.5.551과 동일한 소스에서 빌드). 바뀐 것은 동봉 스킬(`bundled-skills/vue3-migration`)과 문서입니다. 키트와 체크섬(`SHA256SUMS.txt`)이 정식으로 전달되고 검증되기 전에는 기존 설치에 이 절차를 적용하지 마십시오.

## 이 키트가 담는 것

- v1.5.551(전날 키트)의 내용 전체 — 사고 모드 출력 걸러내기, 실패 초안 복원, 터미널 UI 4라운드 — 에 더해, **Vue 3 마이그레이션 스킬을 실전 전환에서 배운 내용으로 크게 확장**했습니다(소스 기준 v1.5.551 → v1.5.552 누적 통합). v1.5.551을 아직 설치하지 않았다면 이 키트만 설치하면 됩니다.
- 구성은 이전 정식 키트와 동일합니다: 클라이언트(njh-cli), 게이트웨이(njh-gateway), 노트북 검증 킷(njh-win-laptop), Ollama Windows CPU 런타임, OCR 엔진, 빌드 매니페스트, SHA-256 매니페스트.
- 설치 절차와 현장값 관리 방식은 바뀌지 않았습니다. 현장값은 `~/.njh-cli/site.env` 한 파일에만 두고, 설치 명령은 용량 확인 → 전체 사전 점검 → 실제 설치 순서를 그대로 따릅니다.

## 수신자 관점의 주요 변화

- **Vue 3 마이그레이션 스킬 확장**: 참조 문서 20 → 87본, 스크립트 25 → 177개, 합성 픽스처 76 → 350파일. 실제 사내 규모 Vue 2 프로젝트(라우트 76개)를 Vue 3 + Vuetify 3로 전환하면서 필요했던 규칙과 자동화가 그대로 들어 있습니다. 규칙 카탈로그(`references/09-case-catalog.json`)는 70케이스이며, 그중 53케이스에 실행 가능한 검증기 또는 코드모드 소유자가 있습니다.
- **새 코드모드 3종**: 이벤트버스 리스너 수명주기(`vue3_event_bus_lifecycle_codemod.mjs`), 엑셀 업로드 그리드 옵션 공통화(`vue3_excel_upload_grid_options_codemod.mjs`), Options API 내비게이션 push 경계(`vue3_navigation_push_codemod.mjs`). 세 가지 모두 dry → apply → 다시 dry(변경 0) 순서로 픽스처 검증을 통과했습니다.
- **AS-IS ↔ TO-BE 인벤토리 생성기**(`vue3_asis_tobe_inventory.mjs`): 두 git ref 사이의 실행 소스 경로를 전환됨·동일·보류·신규·삭제 5상태 × 스크립트/LLM 보조/수작업 3레인으로 한 번씩만 집계해 `analysis/asis-tobe-inventory.{json,md}`로 씁니다. 전환 진척과 미결 파일을 사람이 세지 않아도 됩니다.
- **파서 패키지 해석 규칙**: `@babel/parser`, `@babel/types`, `@babel/generator`, `@vue/compiler-sfc`를 쓰는 스크립트는 먼저 **마이그레이션 대상 프로젝트**(명령을 실행한 디렉터리의 `package.json`)에서 패키지를 찾고, 없으면 스킬 위치에서 찾습니다. 둘 다 없으면 어느 두 곳을 봤는지 이름과 함께 알려 주므로, 대상 프로젝트 루트에서 실행하십시오.
- 스킬 테스트(`bundled-skills/vue3-migration/scripts/*.test.mjs`)는 마이그레이션 랩 없이도 동작합니다. 랩 미러가 없으면 미러 대조 테스트 8건은 사유를 남기고 건너뜁니다.

## 같은 로컬 모델에서의 비교 결과(참고)

같은 `gemma4:12b`(Ollama, 루프백) 로 Vue 3 마이그레이션 과제 4개를 돌린 결과, njh-cli 는 4개 모두 완주하고 검증 단언 194/194 를 통과했으며 외부 네트워크 접속 시도가 0건이었습니다. 비교 대상(OpenCode 계열)은 스킬 없이 0/4, 같은 스킬을 붙여도 2/4 였고 실패 원인은 모두 폐쇄망 규칙 위반(외부 주소 접속 시도)이었습니다. 통과한 같은 과제 2개 기준으로 벽시계 시간은 약 1/5, 토큰은 약 1/3 이었습니다. 측정 조건(단일 모델·단일 호스트·과제당 1회)이 제한적이므로 절대값보다는 "완주·검증·폐쇄망 준수" 축의 차이로 읽으십시오.

## 설치 전에 확인할 것

- 이전 키트에서 안내한 사전 점검·보관·거부 규칙이 그대로 적용됩니다. 처음 설치하거나 절차가 기억나지 않으면 키트에 동봉된 `통합-설치가이드`와 `02-설치-및-업데이트` 문서를 먼저 읽으십시오.
- `02-설치-및-업데이트.md`와 `06-post-import-first-30-minutes.md`의 `CAPACITY REFUSE` 대응 절차(재시작 금지, 출력 수치로 모델·컨텍스트·JVM 배치 조정 후 재실행)는 v1.5.551과 동일하게 유지됩니다.
- 기존 서비스 디렉터리는 삭제하거나 덮어쓰지 않고 시각이 붙은 형제 디렉터리로 보존되며, Ollama 모델 저장소는 그대로 유지됩니다.

## 설치 후 확인

설치 명령이 성공하면 Ollama 상태를 확인하고, Gateway 역할이 포함된 서버에서는 보관 디렉터리 존재 확인 후 Gateway smoke를 실행하십시오. 클라이언트에서는 `njh --version`이 `1.5.552`를 출력하는지, `njh ledger status --since 7d`가 오류 없이 표를 출력하는지, `/skills` 목록에 `vue3-migration`이 보이는지 확인하십시오. 마이그레이션 대상 프로젝트 루트에서 동봉 스킬의 인벤토리 스크립트(`vue3_inventory.mjs`, 스킬 폴더 안 `scripts` 하위)를 `node <스크립트 경로> .` 형태로 한 번 실행해 인벤토리가 생성되면 스킬 동봉이 정상입니다.
