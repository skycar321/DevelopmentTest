# Vue 3 draft kit — 2026-09-20 (77th)

This directory holds one encrypted archive (`vue3-draft-kit-20260920-77.7z`, 7z AES-256 with
encrypted headers) and its checksum. The password is the same as the njh-cli kits and is
delivered separately.

## Verify and extract

```bash
sha256sum -c sha256.txt        # or: shasum -a 256 -c sha256.txt
7z x -p<password> vue3-draft-kit-20260920-77.7z
```

Then read `vue3-carry-20260909-v2/README-사내-반입.md` — its first section "지금 바로 시작하기" is the
command order for a fresh start.

## Requires

- The njh-cli replacement kit **v1.6.10** or later from this channel (`v1.6.10/`).

## Rehearsal

The full chain (00-env → 01 → 02 → 03 → 04 → 05 → 06, then a fresh start with 0c and the chain again)
was run on the lab tree before packaging; result **PASS**.

## What changed

- **눈에 보이던 글꼴 차이를 원인까지 규명해 되돌렸다 — 22단계 새 분류 `css-restore`.** 목표 UI 라이브러리(4.x)는 CSS **캐스케이드 레이어**를 쓰고 이전 판(2.x)은 쓰지 않는다. 레이어 밖(작성자) CSS 는 레이어 안 CSS 를 이기므로 **앱이 직접 쓴 CSS 는 여전히 이긴다** — 문제는 라이브러리 **내부** 순서였다. 부품의 글꼴 규칙보다 **리셋**(`button, input, optgroup, select, textarea { font: inherit; }`)이 뒤에 와서, 명시도와 무관하게 리셋이 이긴다. 그래서 `<button>` 으로 그려지는 부품(버튼·탭)의 글꼴이 **부모에서 상속된 값**으로 바뀐다(탭: 굵기 500·줄 높이 normal → 400·22.5px). **그 선언 하나만 빼고 되돌리는 대조 실험**으로 격리했고, 규칙을 하나씩 넣어 재고 빼고 마지막에 합쳐 다시 쟀다.
- **측정치(랩, 브라우저 8개 상태·의미로 짝지은 요소 696개): 대상 차이 173 → 0 · 새로 생긴 차이 0 · 기하(위치·크기) 차이 179 → 179(새로 생긴 것 0) · 전체 비교 가능 속성 차이 8,527 → 8,354.** 세 자리(상단 메뉴 버튼 80건 · 탭 50건 · 대시보드 작업 카드 버튼 43건)에 각각 스타일 한 개씩 들어간다. 값은 **이전 판에서 실제로 보이던 계산값**이고 새로 고른 디자인이 아니다. 대시보드의 높이 차이는 줄 높이의 결과라 **높이를 고정값으로 넣지 않았다**. 생산 빌드에서도 규칙 세 개가 **레이어 밖·`!important` 없이** 나오는지 확인하는 점검 명령을 함께 넣었다.
- **수단으로 쓰지 않은 것을 분명히 한다.** `!important` 를 쓰지 않았고(레이어 우선순위를 뒤집어 앱 CSS 와 라이브러리의 관계를 통째로 흔든다), 레이어 평탄화·순서 변경도 하지 않았다(옛 대조군이고 이 분류의 수단이 아니다). 전역 `button`·버튼 클래스·탭 역할 선택자도 쓰지 않았다 — **재지 않은 화면을 바꾸지 않기 위해** 실측한 세 자리에만, 그 화면의 실제 루트 아래로 건다. 위치로 찍는 선택자(`:nth-child`) 대신 루트에 표지 속성 **한 개**만 붙인다.
- **자동으로 덮지 않는다(거부 안전).** ① 설치본이 레이어 안에서 버튼 글꼴을 리셋하고 있어야 전환한다(전제가 없으면 분류 전체를 건너뛴다). ② 측정한 자리의 구조 표지가 템플릿에 없으면 바꾸지 않는다 — 이름 경계로 보기 때문에 클래스 이름이 바뀐 화면은 "같은 구조" 로 세지 않는다. ③ 화면 스타일이 **이미 그 자리 글꼴을 정해 두었으면** 손대지 않는다(값이 다를 수 있다). ④ 표지 속성을 붙이는 규칙은 루트 모양이 측정 때와 같아야 한다. 어긋나면 **사유와 함께 사람 확인으로 남긴다.** 두 번 돌리면 아무것도 바꾸지 않고(멱등), 전환 뒤 빌드가 깨지면 이 분류만 되돌린다.
- **감추지 않은 것.** 남은 8,354개 속성 차이·179개 기하 차이·짝 없는 노드·닫힌 오버레이·hover·다른 권한 상태는 **그대로 남아 있다** — 각자 원인 규명과 디자인 승인이 필요하다. 측정한 상태는 **개발 서버 브라우저 8개 상태**이고, 생산 빌드는 CSS 추출 결과를 파일로 확인했을 뿐 **생산 브라우저에서의 레이어 해석·지연 청크 순서는 재지 않았다**. 그리고 그 8개 상태는 모두 **표 데이터 행이 0개**였다 — 행 여백·줄무늬·선택 강조는 이 복원으로 증명된 것이 아니다.
- **"권한으로 막힌 화면은 결함이 아니다" 를 전수 측정으로 확정했다.** 라우터 소스에서 뽑은 **77개 화면을 두 트리(전·후) 양쪽 전수**로, **모든 메뉴·버튼 권한을 열고 합성 행 125개**를 넣고 팝업에는 소스에 적힌 입력을 심어 다시 쟀다. 결과: **전환 후에서만 실패한 화면 0개**(마운트 못 한 것을 실행 예외까지 넓혀 세어도 0). 양쪽 실패 3건은 로컬 로그인 로딩 흐름 2건과 템플릿이 없는 샘플 1건이다. 행·선택·페이지 스타일은 보이는 셀 전부와 행·페이지 영역까지 **13,662건 비교해 차이 0** 이었다. 그래서 **사내에서 화면 일부만 뜨는 것은 권한·데이터 조건일 수 있고 전환 결함이 아니다** — 이 문장이 스윕 보고서·담당자 배정 명단·사진용 보고(23단계)·동등성 비교(17단계) 안내에 **사실로 찍힌다**.
- **그래도 판정 기준은 그대로 이전 판과의 비교다.** "전환 후에서만 안 뜸" 이 나오면 그것이 배정 대상이고, 조건이 다른 비교는 무효라 명단에서 아무것도 빼지 않는다. 랩에서 권한을 다 연 조건으로도 전용 실패가 0 이었으므로, 사내에서 전용 실패가 보이면 **사내 데이터·권한·운영 변경**을 먼저 본다. 권한을 열고 행을 넣는 방식은 **응답을 만들어 넣는 것**이라 스윕 단계에 붙이지 않았다 — 사내 스윕은 실제 앱이 실제 응답으로 뜨는지를 재야 한다. 방법과 하네스는 재현 가능한 형태로 킷 안에 남겨 두었다.
- **게이트 보강.** 전체 사슬 리허설이 ① 새 분류가 첫 실행에서 커밋을 만들고 두 번째 실행에서는 바꿀 것이 없는지, ② 넣은 스타일에 `!important`·레이어 조작·scoped 가 없고 실측 규칙이 그대로 들어갔는지, ③ **생산 빌드 결과 CSS** 에서도 레이어 밖·`!important` 없이 나오는지, ④ 덮어서는 안 되는 화면(이미 글꼴을 정해 둔 화면·구조가 바뀐 화면)을 **실제로 거부하는지**, ⑤ 전수 측정 사실이 세 산출물에 실제로 찍히는지(합성 입력으로 보고서를 만들어 본다), ⑥ 킷에 담긴 측정 감사·판정 JSON 이 문서의 주장(대상 차이 0·새 차이 0·전용 실패 0)과 같은지, ⑦ 반입 절의 명령 순서가 계약과 같은지를 확인한다. 새 단언은 **개수를 박는 대신 "있는지 · 0 인지 · 줄었는지"** 로 썼다.
- 킷 도구 테스트 전수 **253건 통과 · 실패 0 · 건너뜀 0**(35개 파일). 76차까지의 변경 포함.
