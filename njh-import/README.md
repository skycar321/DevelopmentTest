# NJH encrypted recipient-delivery channel

Current immutable release: [v1.5.554/](./v1.5.554/)

v1.5.554 source: `main` @ `1353ffb4af` (2026-09-06). Adds the OpenAI-compatible server transport
(`openai-import` profile, one DevAI key), `/goal` standing goals and `njh mission` detached runs,
the headless approval contract (exit 2), sub-agent role profiles with a shared concurrency cap,
and the East Asian ambiguous-width status-bar fix. Previous immutable release: [v1.5.552/](./v1.5.552/)

The version directory and this index are published in one atomic commit.
Existing version directories are immutable and must never be replaced.
