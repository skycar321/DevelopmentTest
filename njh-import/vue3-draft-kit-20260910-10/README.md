# Vue 3 초안 킷 — 10차

암호 7z(AES-256, 헤더 암호화). 암호는 njh-cli 킷과 같고 별도 채널로 전달된다.

## 이 판에서 바뀐 핵심 — 상태를 킷 밖으로 뺐다

지금까지는 설정(`env.sh`)과 진행 상태(보고서·로그·`draft-hashes.txt`)가 **킷 폴더 안**에 있었다.
그래서 킷을 새로 받을 때마다 이사가 필요했고, 이사를 잊고 `00-env.sh` 를 다시 돌리면
`BASE_TAG` 가 오늘 날짜로 새로 생겨 **초안의 출발점이 조용히 옮겨갔다**(그러면 "손댄 화면" 을 가려낼 수 없다).

10차부터 설정·진행 상태는 **`~/.vue3-draft/`** 에 있다. 킷은 갈아 끼우는 물건으로만 쓴다.

```bash
# 새 터미널을 열 때마다 첫 줄 — 이것 하나면 된다
source "<킷 경로>/kit.sh"

bash "$KIT/run/00b-resume.sh"     # 어느 단계부터 하면 되는지 한 줄로 찍는다(읽기 전용)
```

`kit.sh` 는 **자기 위치에서** `KIT` 을 정하므로, 새 킷을 아무 폴더에 풀어도 값이 항상 맞다.
설정에 킷 경로를 적지 않으니 고칠 것도 없다.

## 받는 법

```bash
sha256sum -c sha256.txt          # 또는 shasum -a 256 -c sha256.txt
7z x -p'<암호>' vue3-draft-kit-20260910-10.7z
```

풀면 나오는 `vue3-carry-20260909-v2/` 안의 **`docs/뷰3-초안-사내-런북.md`**(같은 내용의 `.html`)가 절차 정본이다.
HTML 은 외부 참조가 없는 단일 파일이라 폐쇄망에서 그대로 열린다. 단계마다 `run/NN-*.sh` 한 줄이다.

## 9차 이하로 작업하던 중이라면 한 번만

```bash
bash "<새 킷>/run/00a-adopt-kit.sh" <옛 킷 경로>
```

옛 `env.sh` 의 값(`WEB`·`BASE_TAG`·`NPM_REGISTRY` …)을 **한 글자도 바꾸지 않고** 홈으로 옮기고,
진행 상태도 함께 옮긴다. 이 뒤로는 킷을 몇 번 바꾸든 이 단계가 다시 필요 없다.

## 그 앞 판들에서 들어온 것

- **초안 빌드가 통과한다.** 변환 여럿이 한쪽 실행 경로에만 배선돼 있어, 실제 프로젝트가 타는 경로에서
  빌드가 깨지고 완전 전환이 0으로 집계됐다. 배선 12종을 채우고 **두 실행 경로가 같은 변환을 타는지
  검사하는 계약 테스트**를 넣었다. 앞으로 배선이 빠지면 빌드가 아니라 테스트가 먼저 깨진다.
- **공통 부품이 전부 착지한다.** 채택하는 화면이 없어 파일 자체가 안 깔리던 부품 8종을 정본 경로에
  직접 넣는다(화면은 건드리지 않는다). 다른 설치자가 주인인 경로는 덮어쓰지 않고 거부로 기록한다.
- **게시 단계 합격 관문 3개** — `npm run build` · `npm run serve` 기동 응답 · **공통 부품 착지 검사**.
- **화면이 실제로 뜨는지 잰다** — 빌드 통과와 서버 응답은 화면이 살아 있다는 증거가 아니다
  (import 를 지우고 템플릿을 비워도 둘 다 통과한다).
  - `run/08a-login-session.sh` — 담당자가 브라우저에서 직접 로그인한다(아이디·비밀번호는 킷도 스크립트도 받지 않는다).
  - `run/08b-route-sweep.sh` — 실제 브라우저로 라우트를 하나씩 열어 마운트 여부를 판정한다. 브라우저는
    내려받지 않고 이미 깔린 Edge/Chrome 을 쓰며, 순회 중 GET/HEAD 외의 요청은 차단한다.
  - `run/08c-screen-repair.sh` — 안 뜨는 화면을 하나씩 보수하고 **다시 재서** 실제로 떴는지 확인한다.
    끝내 안 뜨는 화면은 `화면-담당자-배정.md` 로 나온다.
- `run/05a-ai-repair.sh` — 빌드가 막히면 CLI 에이전트가 차단 파일을 하나씩 보수한다. 기능을 지워
  통과시킨 수정은 스크립트가 되돌린다(대상 외 파일 변경도 되돌린다).
- 런북에 **"njh-cli 는 어디에 쓰나"** 표 — 단계별로 무엇을 시키고 **무엇이 그 결과를 판정하는지**.

## 함께 쓰는 것

njh-cli 는 채널의 현행 버전 디렉터리에서 받는다(런북 0단계가 버전을 적어 둔다).

## 보내 주면 좋은 것

`~/.vue3-draft/` 의 `draft-facts.md` · `draft-review.md` · `ai-repair.md` · `route-sweep.md` · `화면-담당자-배정.md`.
로그인 세션 파일(`.route-session.json`)은 보내지 않는다.
