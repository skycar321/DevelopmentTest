# njh-cli 교체 키트 릴리스 노트 — v1.5.557 (2026-09-09)
이 문서는 `v1.5.557` 교체 키트와 함께 전달됩니다. v1.5.556 직후의 통합판이며 **실행 파일이 바뀌었습니다**(클라이언트 번들 재빌드, 게이트웨이 번들은 동일 소스 재빌드). 키트와 체크섬(`SHA256SUMS.txt`)이 정식으로 전달되고 검증되기 전에는 기존 설치에 손대지 마십시오.
 <!-- test-derived-document-contract:start; generated from executable test assertions; LOAD-BEARING document lines=1@scripts/user-guide-sync.test.ts:3113,11@scripts/user-guide-sync.test.ts:3114,12@scripts/user-guide-sync.test.ts:3117; run npx tsx scripts/recipient-document-contract.ts --write after changing this document or its test; test-derived-document-contract:end -->
## 이 키트가 담는 것

- v1.5.556 의 전체(사내 OpenAI 호환 연결·`/goal` git 쓰기 옵트인·Vue3 마이그레이션 스킬·Windows Terminal 프로필·비대화형 첨부와 스킬 지정)에 더해, **첫 사내 실행 캡처(2026-09-08)에서 남아 있던 화면 결함의 후속 수정과 Vue3 스킬의 2차 증분(ExcelJS 위상 계약·공통 로딩 오버레이·검증된 미션 절차 문서)** 이 들어갔습니다.
- 구성·설치 절차·현장값 관리 방식은 v1.5.556 과 같습니다.

## 수신자 관점의 주요 변화

- **시작 직후 "▲ 런타임 경고 1건 · /alerts" 가 더 이상 뜨지 않습니다.** 원인은 Node 의 `node:sqlite` 실험 기능 경고였고, 지식 저장소는 실제로 처음 쓰이는 순간에만 열리도록 바뀌었습니다. 알려진 실험 경고는 `/alerts` 상세에만 정보로 남고 `/doctor` 가 한 줄로 알려 줍니다.
- **터미널이 보내는 제어 시퀀스가 붙여넣기로 오인되지 않습니다.** 마우스·커서 보고 시퀀스와 CR/LF 는 붙여넣기 판단 기준에서 제외됩니다.
- **상태줄이 유휴·대기·스트림·도구·차단·종료 상태와 문맥 출처·권한/정책·대기 건수·더티 상태·복구 힌트를 한 줄로 보여 주고, 승인 선택과 상태 게이지에 의미 색상·상태 표식이 붙으며, 세션 종료 시 검증된 요약이 남습니다.** `/model` 에서 Ollama 제공자 조회 실패는 선택 불가 행으로 드러납니다.

## 서버에서 돌리기 전에

- Node.js 요건 `22.13.0+` 는 변함이 없습니다. 미션 절차·git 쓰기 정책·첨부 점검 문서는 v1.5.556 과 동일한 정본을 씁니다.
- 키는 환경 변수에만 넣고 설정 파일에는 환경 변수의 이름만 둡니다.

## 설치 전에 확인할 것

- 이전 키트의 사전 점검·보관·거부 규칙이 그대로 적용됩니다. 처음 설치하거나 절차가 기억나지 않으면 `통합-설치가이드`와 `02-설치-및-업데이트` 문서를 먼저 읽으십시오.
