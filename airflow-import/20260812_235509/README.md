# airflow-bundle 20260812_235509  ← 반입 권장본

배치 어드민 Airflow 제어 화면 + Airflow 번들 반입본입니다. 압축은 헤더까지 암호화(`-mhe=on`)되어 있고 비밀번호는 이 저장소에 없습니다.

```bash
7z x airflow-bundle-20260812_235509.7z
tar -zxvf airflow-bundle.tar.gz
shasum -a 256 -c SHA256SUMS.txt
```

## ⚠ 반영 전제 — 재빌드가 필요합니다

CSS·HTML·JS 변경이 크므로 **원본 앱을 war/jar 로 재빌드·재배포**해야 화면에 반영됩니다. **이번 판부터 jQuery 실행본(`js/jquery/dist/jquery.js`)이 번들에 동봉됩니다** — 기존에는 저장소에 소스만 있고 실행본이 없어 환경에 따라 화면 전체가 동작하지 않을 수 있는 반입 결함이었습니다.

## 141950 대비 — 실제 앱 검증 체제에서 나온 개선판

이번 판의 모든 화면 변경은 합성 픽스처가 아니라 **가데이터로 기동한 실제 배치어드민 앱(1920×1080 최대화)** 에서 확인했습니다.

### 사용자 요청 반영
| 요청 | 반영 |
|---|---|
| DAG 목록 컬럼 정리 | 11열 → **9열** (활성여부+최근실행 → 상태 / 실행요청+스케줄변경 → 요청), 태그 폭 2배 |
| `실행 경로` 명칭 모호 | **`실행 방식`** 으로 개명 + 상세 설명문 재작성 |
| subflow 노드 펼치기 | 빌더에서 ▸/▾ 로 재귀 전개, 순환 참조 차단 포함 |
| 화면 캡처 기준 | 1920×1080 최대화 단일 기준으로 전 검증 재정렬 |

### 디자인 전수감사(신설) 반영분
| 항목 | 바뀐 것 |
|---|---|
| 반려 알림 오독(심각) | 성공과 같은 연청 카드였던 반려·실패 알림 → **적색 계열 카드 + 반려 마커** |
| DAG 상세 | raw JSON(내부 필드) 우선 → 운영 요약 우선, JSON 은 접힌 고급 메타데이터로 |
| 자동변환 막힘 화면 | 같은 오류 문구 2회 반복·복구 불가 → 단일 안내 + 복구 동선 |
| 실행 방식 모달 | 전 항목 비활성인데 모달이 열려 취소·닫기 두 버튼만 남던 것 → 진입 차단 + 인라인 사유 |
| 빌더 Task 설정 | 물음표 도움말 접기를 전 필드로 확장(인라인 요약이 있는 필드만 접음) |
| 투어 진입 메뉴·목차 | 좌측 정렬 정규화(시작점 16종→1종), 목차 버튼 스타일 통일 |

### 검증 상태
- 패키징 검증 `verify_bundle` 전항 PASS.
- 정본 화면 스위트 **49 PASS / 3 FAIL / 1 HOLD** — 남은 3건은 화면 동작 결함이 아니라 검사 장치의 픽스처-실제 앱 불일치이며 수리 중입니다. 실제 앱 6화면 실측은 깨짐·겹침·가로 넘침 0건입니다.
