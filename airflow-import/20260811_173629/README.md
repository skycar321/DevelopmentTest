# airflow-bundle 20260811_173629

배치 어드민 Airflow 제어 화면 + Airflow 번들 반입본입니다. 압축은 헤더까지 암호화(`-mhe=on`)되어 있고 비밀번호는 이 저장소에 없습니다.

```bash
7z x airflow-bundle-20260811_173629.7z
tar -zxvf airflow-bundle.tar.gz
shasum -a 256 -c SHA256SUMS.txt
```

## 165229 대비 — 승인 화면을 마저 다듬었습니다

- **DAG 열**: 90자가 넘는 식별자가 열을 가로로 넘치던 것을 한 줄 말줄임으로 바꿨습니다. 전체 값은 셀의 `title` 에 그대로 있어 마우스를 올리면 보입니다.
- **열 폭 재배분**: 변경내용 33% / DAG 12% / 처리자 9%, 마지막 `처리` 열은 자동 폭으로 두어 반올림 잔차를 흡수합니다. 이전 판은 전 열을 퍼센트로 줘서 1280 폭에서 4px 가로 넘침이 있었습니다.
- 1440×900 기준 한 화면에 보이는 승인 건수는 **6건 → 10건 이상**입니다(이전 판에서 달성, 이번 판 유지).

## 검증 상태

정본 스위트 **45 PASS / 2 FAIL / 1 HOLD**. 두 건 모두 화면 사용을 막지 않습니다.

| 미결 | 내용 |
|---|---|
| `airflow-visual-geometry-sweep` | 1280×720 에서 한 화면에 보이는 승인 건수가 7건입니다(기준 8건). 1440×900 은 기준을 넘겼고, 좁은 해상도만 한 칸 모자랍니다. |
| `airflow-list-processing-lifecycle` | 실행 완료 후 포커스가 원래 셀로 돌아가지만 그 셀의 중심 좌표가 화면 밖이라 "완전히 보임" 판정이 서지 않습니다. 실행·중복 제출 차단은 정상 동작하며 검증도 통과합니다. |
