# airflow-bundle-20260826_135915 반입 안내

- 소스 커밋: `f303b1b6b` (main)
- SHA256: `b2a11a7b371f1194afa18da6cd5fe3df9c3ad1d7838eb999fb230f0fbc7c12a0`
- 이전 반입판(20260826_121742) 대비 변경:
  1. **DAG 목록 필터 전면 일반화** — DAG ID 포함 모든 표시 헤더(태그·실행주기·활성여부·최근실행상태·실행요청 등)에 엑셀식 필터. 드롭다운 안 타이핑 검색 + 검색결과∪체크값 union, "모두 선택"=현재 보이는 부분집합, 모든 컬럼에 `(없음)` 빈값 버킷(실행주기 없는 DAG도 필터 가능), 새로고침 시 필터 상태 보존, Tab/Arrow/Esc 키보드 접근성, 600 DAG 규모 성능 확인.
  2. 내부 품질 수리 반입 — 승인/운영 요청 재시도 라벨 정합, 스케줄 안내 문구의 `@daily` 등 프리셋 표기 보존(한글 메시지 검증기 화이트리스트), 검증 게이트 다수 현행화.
- 배포 절차 변동 없음: `./scripts/deploy.sh dev` 만으로 배포(후보 ID 자동 유도 포함, 20260826_072001 판부터 동일).
- 해제: `7z x airflow-bundle-20260826_135915.7z` (비밀번호) → `tar -zxvf airflow-bundle.tar.gz`
- 참고: 로그 오류 점검가이드 기능은 이 판에 미포함(별도 구현 진행 중, 다음 판 반입 예정).
