# Airflow 제어 반입판 — 2026-08-26 3차 (**UI 수리 일괄 + 엑셀식 필터**)

- 번들: `airflow-bundle-20260826_112055.7z`
- source commit: `bb177d15d`
- 직전 판 `20260826_105506`(배포 무입력화) 위에 UI 수리·기능을 얹은 판입니다.

## 오늘 제보하신 화면 문제 전부 반영

| 제보 | 수리 |
|---|---|
| 상세 모달 "확인 필요…변경/자동 변환 검토" 난잡 | 배지 1 + 검토 1버튼, 사유는 툴팁/스크린리더로 |
| 승인/반려 버튼 두 줄 개행 | 한 줄 고정(nowrap+열 최소폭) |
| 실행 방식 "확인 필요+검토" 개행 | 동일 수리 |
| 확인창 '계속 준비' 중복+빈 버튼 | 원인(공유 모달 잔존 retry 버튼) 제거 — 이제 주버튼 1 + 닫기 + 우상단 X (Esc 동작) |
| 기동 시 로그 도배(autogen WARN 반복) | INFO 강등 + 스냅샷당 1회 + 요약 건수 라인 |

## 신규 기능 — DAG 목록 엑셀식 필터

- 통합검색: 타이핑으로 DAG ID·이름·설명·태그 포함 검색
- 열 머리글 필터: 상태/유형 등 값 체크박스(모두 선택 포함, 검색과 AND 결합)
- "선택한 항목만 보기": 체크한 행만 남기고 보기 / 전체 보기 복귀
- 목록 자동 새로고침에도 필터 유지

## 반입 방법 (무입력)

    7z x airflow-bundle-20260826_112055.7z && tar -zxvf airflow-bundle.tar.gz
    BASE_DIR=/data/lowcode/aa CONTAINER_RUNTIME=podman NEED_SUDO=true ./scripts/deploy.sh dev

배치어드민 델타는 java+js+css+html 전부 교체하십시오(css 구판이면 이번 수리가 화면에 안 보입니다).
