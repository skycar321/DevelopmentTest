# ⛔ 이 판은 사용하지 마세요 — `20260811_205721` 을 쓰세요

이 아카이브의 `airflow.css` 에 **병합 충돌 표시가 그대로 남아** 목록 화면 스타일 일부가 무효화됩니다.
표 열 폭 규칙이 적용되지 않아 화면이 의도와 다르게 보입니다. 다음 판에서 해소했습니다.

---

# airflow-bundle 20260811_195718

배치 어드민 Airflow 제어 화면 + Airflow 번들 반입본입니다. 압축은 헤더까지 암호화(`-mhe=on`)되어 있고 비밀번호는 이 저장소에 없습니다.

```bash
7z x airflow-bundle-20260811_195718.7z
tar -zxvf airflow-bundle.tar.gz
shasum -a 256 -c SHA256SUMS.txt
```

## ⚠ 반영 전제 — 재빌드가 필요합니다

이번 판 변경은 **CSS·HTML·JS** 중심입니다. 배치어드민은 원본 앱에 델타를 얹는 구조라 파일만 덮어써서는 Java 외 자원이 반영되지 않을 수 있습니다. **원본 앱을 war/jar 로 재빌드·재배포**해야 화면에서 확인됩니다(`docs/IMPORT_ACTIONS_LATEST.md`). 재빌드 없이 열면 이전 화면 그대로 보이니, "변경이 안 왔다"고 판단하기 전에 이 절차부터 확인해 주세요.

## 173629 대비 바뀐 것

### 목록 화면 — 태그를 읽을 수 있게
보내주신 사진에서 태그가 `act…` `da…` `op…` 처럼 앞 두세 글자만 보였습니다. 헤더 폭이 **48px** 로 못박혀 있어 한글 두 글자도 들어가지 않았고, 태그는 통합검색 대상이라 사실상 검색 기능이 죽어 있었습니다.
- 태그 열 **48 → 88px**, 부족분은 `실행 경로 230 → 190px` 에서 회수했습니다.
- 표가 `width:100%` + 고정 레이아웃이라 **넓힌 만큼 다른 열을 뺏습니다**. 그래서 회수한 40px 범위 안에서만 늘려 다른 열이 좁아지지 않게 했습니다. 더 넓히려면 가로 스크롤 도입이 필요하며, 별도로 검토 중입니다.

### 온보딩 투어 — 안 뜨던 회귀를 사전 차단
검증 중 **투어 창이 아예 열리지 않는 회귀**를 발견해 반입 전에 되돌렸습니다. 저장된 투어를 이어받는 경로에서 투어 루트가 만들어지지 않는 문제였고, 지금은 정상입니다.

## 검증 상태

정본 스위트 **45 PASS / 1 FAIL / 1 PRECOND / 1 HOLD**.

| 미결 | 내용 |
|---|---|
| `airflow-visual-geometry-sweep` | 승인 표 기하 검사 1건. 화면 사용에는 지장이 없습니다. |
| `airflow-list-processing-lifecycle` | 실행 완료 후 포커스 복원 판정이 환경 전제(창 포커스)로 결론을 못 냅니다. 제품 결함이 아닙니다. |

보내주신 화면 피드백 9건 중 태그 잘림을 우선 처리했고, 시나리오 목록 정렬·한글 단어 줄바꿈·용어 풀이가 다음 순서로 진행 중입니다.
