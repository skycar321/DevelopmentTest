# Airflow 제어 반입판 (batch-admin + airflow-bundle)

- 번들: `airflow-bundle-20260816_184249.7z` (1,151,393 bytes)
- sha256: `2d91457785a15046009f04cce5f718b21a27cf795c09b3c0d944232f25968790`
- 직전 반입판: `20260815_015223`

## 1. 배치어드민(웹 화면) 반영
1. `airflow-bundle-20260816_184249.7z` 를 **전체 해제**합니다(비밀번호 필요).
2. 해제된 `batch-admin-reference/src/**`, `mapper/`, `static/`, `templates/`, `sql/` 을 사내 `ui-batchadmin-main` 원본 경로에 덮어씁니다.
3. **jar 를 재빌드**해 배포합니다. (⚠ Java 만 교체하면 이번 판 변경의 상당수가 반영되지 않습니다 — css/js/html/mapper.xml 이 함께 바뀌었습니다.)

## 2. Airflow 서버 반영
같은 7z 안의 `airflow-bundle.tar.gz` 를 Airflow 서버에 올려 해제한 뒤:

```bash
tar -zxvf airflow-bundle.tar.gz
cd airflow-bundle
./scripts/deploy.sh dev
```

`BASE_DIR`·`CONTAINER_RUNTIME`·`NEED_SUDO` 는 **자동 감지**되므로 따로 지정하지 않아도 됩니다.

## 3. DB 반영 (최초 1회)
```bash
psql -f sql/airflow_control_ddl.sql
psql -f spring-batch-reference/sql/dag_task_map.sql
```
두 스크립트 모두 **멱등**이라 이미 적용된 환경에서 다시 실행해도 안전합니다.

## 4. 이번 판 변경 요약 (사용자 관점)

| 구분 | 내용 |
|---|---|
| 목록 화면 | 머리글에 **열 이름표**를 달아 열을 자리가 아니라 이름으로 집도록 정리. 목록 끝 행이 고정 머리글 아래에 영구히 가려지던 문제를 **하단 여백 확장**으로 해소(되감기 방식 폐기) |
| 승인 화면 | 실행 허가 요청 열에서 **"준비 취소" 문구가 잘리던 것** 수정 |
| 배치 만들기(빌더) | 보조 단계가 예외를 던지면 **Task 설정 모달이 아예 열리지 않던 것** 수정. Task 추가에서 여는 호출에 도달하지 못하던 경로 차단 |
| 상세 가이드 | 좌측 목차의 한글이 **음절 단위로 줄바꿈**되던 것 수정(예: "찾고 싶/어요" → "찾고 / 싶어요") |
| 사용자 문구 | 조사(을/를·이/가) 오류 정정을 공용 유틸로 통일. 실행 방식이 확인되지 않을 때의 안내 문구 명확화 |
| 세션·보안 | 로그인 세션 처리 경로 하드닝 |

## 5. 검증 결과 (반입 전)

측정 환경은 실제 앱(샌드박스 9999)이며, 화면 기준은 **1920x1080 브라우저 최대화**입니다.

| 축 | 결과 |
|---|---|
| 브라우저 게이트(정본 스위트) | **67 PASS / 1 FAIL / 1 HOLD** |
| Java 통합 테스트 (maven 축) | **d46·d51 전건 통과** (ApprovalTxIT, 오류 0) |
| Python 회귀 (pytest 축) | **d43·rt 전건 통과** |
| 실앱 육안 검수 1920 | **6/6 화면**(목록·승인 3탭·빌더·가이드), 오류 배너·로드 실패 **없음** |
| 번들↔샌드박스 패리티 | 일치(환경 전용 2파일 제외) |

## 6. 알려진 열린 항목 (이번 판에 포함되지 않음)

반입을 막지 않는다고 판단한 항목을 숨기지 않고 적습니다.

| 항목 | 관측값 | 판단 |
|---|---|---|
| 화면 안내(투어) 강조 띠와 목차 패널이 **2.75px 겹침** (1920) | TOC 좌변 1592.00 / 강조 띠 우변 1594.75 | 육안으로 식별되지 않는 머리카락 두께 충돌. 다음 판에서 해소 |
| 그래프 안내 단계에서 **말풍선이 대상 중심을 가림** (1920) | 대상 384~1536 / 말풍선 533~973 / 중심 (960,535) | 대상이 화면 폭 전체라 회피 여지가 좁음. 다음 판에서 배치 조정 |
| 축소 폭 관측 3건 | 1024 상태 배지 축약(`관…`)·"선택" 머리글 2줄, 1366 도움말 목차 계층, 1280 목차 그룹 포함 | **지원 기준(1920 최대화) 밖**. 사내 안내는 "최대화하면 잘 됩니다" |

`HOLD` 1건은 `AIRFLOW_SUITE_PERIODIC_HEADED_TAGS=1` 에서만 도는 헤디드 전용 계약이라 정상입니다.
