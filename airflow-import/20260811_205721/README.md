# airflow-bundle 20260811_205721  ← 이 판을 쓰세요

> **직전 판 `20260811_195718` 은 쓰지 마세요.** 그 아카이브의 `airflow.css` 에 병합 충돌 표시가 그대로 남아 있어 화면 스타일이 깨집니다. 이 판에서 해소했습니다.

배치 어드민 Airflow 제어 화면 + Airflow 번들 반입본입니다. 압축은 헤더까지 암호화(`-mhe=on`)되어 있고 비밀번호는 이 저장소에 없습니다.

```bash
7z x airflow-bundle-20260811_205721.7z
tar -zxvf airflow-bundle.tar.gz
shasum -a 256 -c SHA256SUMS.txt
```

## ⚠ 반영 전제 — 재빌드가 필요합니다

이번 판 변경은 **CSS·HTML·JS** 중심입니다. 배치어드민은 원본 앱에 델타를 얹는 구조라 파일만 덮어써서는 Java 외 자원이 반영되지 않을 수 있습니다. **원본 앱을 war/jar 로 재빌드·재배포**해야 화면에서 확인됩니다(`docs/IMPORT_ACTIONS_LATEST.md`).

## 195718 대비 바뀐 것

- **스타일 깨짐 해소**: 목록 화면 스타일 일부(`@media` 구간)가 병합 충돌 표시 때문에 무효화돼 있었습니다. 표 열 폭 규칙이 적용되지 않아 화면이 의도와 다르게 보였을 수 있습니다.
- 태그 열 폭은 소유 트랙 판(설명·실행 경로에서만 회수)을 채택했습니다. 두 열은 `title` 속성에 전문을 유지하므로 마우스를 올리면 전체 값이 보입니다.

## 그대로 유지되는 개선

- **목록**: 태그가 `act…` 처럼 두세 글자만 보이던 것 → 읽을 수 있는 폭으로.
- **승인 화면**: 한 화면에 보이는 건수 6 → 10건 이상(1440×900), 열 폭을 내용량에 맞게 배분(정보 은닉 없음), `전체 보기` 로 펼친 원문이 잘리던 결함 해소, 제목 5조각 → "운영 변경 요청".
- **자동변환 운영**: 나란한 3버튼 폐기 → 현재 상태 배지 + 단일 "변경" + 선택 모달(승인 안내 포함).
- **오류 문구**: 영문 기술 문자열이 사용자 문구로 새던 것 차단. 단, 형식 힌트·식별자·오류 코드는 그대로 보여 무엇을 고칠지 알 수 있게 했습니다.

## 검증 상태

정본 스위트 **45 PASS / 2 FAIL / 1 HOLD**.

| 미결 | 내용 |
|---|---|
| `airflow-visual-geometry-sweep` | 1280×720 에서 한 화면 승인 건수 7건(기준 8건). 1440×900 은 기준 충족. |
| `airflow-list-processing-lifecycle` | 실행 완료 후 포커스가 원래 셀로 돌아가지만 그 셀 중심이 화면 밖이라 "완전히 보임" 판정이 서지 않습니다. 실행·중복 제출 차단은 정상입니다. |
