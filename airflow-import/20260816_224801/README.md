# Airflow 제어 반입판 (batch-admin + airflow-bundle) — **최종본**

- 번들: `airflow-bundle-20260816_224801.7z` (1154305 bytes)
- sha256: `a849244d79ae28fae7a1af1e23bb9f973634dfe246e8aefedcbd3a96265a4236`
- **이 판을 받으세요.** 같은 날 올린 `20260816_210353` · `20260816_221626` · `20260816_222425` 는 이 판에 모두 포함됩니다.

## 1. 배치어드민(웹 화면) 반영
1. 7z 을 **전체 해제**합니다(비밀번호 필요).
2. `batch-admin-reference/src/**`, `mapper/`, `static/`, `templates/`, `sql/` 을 사내 `ui-batchadmin-main` 원본 경로에 덮어씁니다.
3. **jar 를 재빌드**해 배포합니다. (⚠ 이번 판은 css/js/html 변경이 대부분이라 **Java 만 교체하면 거의 반영되지 않습니다.**)

## 2. Airflow 서버 반영
```bash
tar -zxvf airflow-bundle.tar.gz
cd airflow-bundle
./scripts/deploy.sh dev
```
`BASE_DIR`·`CONTAINER_RUNTIME`·`NEED_SUDO` 는 자동 감지됩니다.

## 3. DB 반영 (최초 1회, 멱등)
```bash
psql -f sql/airflow_control_ddl.sql
psql -f spring-batch-reference/sql/dag_task_map.sql
```

## 4. 이번 판 변경

### 4-1. 사용자 지적 반영 (사진 검토분)

| # | 지적 | 조치 |
|---|---|---|
| 1 | 실행주기가 빌더 위쪽(cron)과 고급 연동·배포(schedule) 두 곳에 있어 왜 두 개인지 알 수 없음 | 같은 설정이 맞습니다(위=`schedules` 복수 키, 고급=옛 `schedule` 단수 키). 고급 쪽을 **`단일 실행주기 (schedule · 옛 형식)`** 로 바꾸고 "위 「실행주기 규칙(cron)」과 같은 설정이며 위에서 편집했다면 여기는 비워 둡니다" 를 설명에 명시 |
| 2 | 상세 모달의 **맞춤** 버튼 CSS 깨짐 | 스타일이 `.airflow-content` 하위로만 걸려 있어 모달에서 기본 버튼으로 떨어진 것. 모달 스코프 추가 |
| 3 | `실행 허가 요청` → **`실행 요청`** | 목록 헤더·버튼·확인 대화상자·투어 문구까지 전량 통일 |
| 4 | `실행주기 변경` → **`스케줄 변경`** | 동일하게 전량 통일 |
| 5-1 | 목록에 `multi-cron:` 개발자 용어 노출 | 표시에서만 접두어 제거. **쉼표 목록은 쪼개지 않습니다** — 요일 목록(`1-5,7`) 오분할을 막기 위해 원본 형태 유지 |
| 5-2 | 탭은 `실행 순서 그림`, 안쪽 제목은 `작업 흐름도` 로 두 이름 | **`작업 흐름도`** 로 통일 |

### 4-2. 함께 고친 것 — 멀티크론 배치의 스케줄을 화면에서 바꿀 수 없던 문제

여러 실행주기를 쓰는 배치(예: `30 2 * * 1-5,7, 0 9 * * 6`)는 **스케줄 변경 창을 열자마자
"cron 표현식이 5필드가 아닙니다" 로 막혀 요청 버튼이 잠겼습니다.** 검증과 제출이 같은 값을
서로 다르게 해석하고 있었습니다. 검증을 제출과 같은 해석으로 맞춰 정상 통과하도록 고쳤습니다.

- 실측: 창을 열면 `✓ 사용 가능한 실행주기입니다 / 유형: multicron` 과 다음 실행 3건이 표시되고 요청 버튼이 열립니다.
- 일괄(여러 배치 한 번에) 변경은 서버가 배치당 하나의 실행주기만 받으므로, **여러 실행주기를 쓰는 배치는 각 행의 `스케줄 변경`** 에서 바꿔 주세요. 일괄 화면 안내문도 그렇게 고쳤습니다.
- `dataset(...)` 스케줄과 `@daily` 프리셋은 이 변환에서 제외되어 영향받지 않습니다.

### 4-3. 자동 생성 DAG 가 목록에 안 보이던 문제 차단 (deploy.sh)

사내 dev 에서 실제로 겪은 증상입니다. `deploy.sh` 실행 후 **DAG 수백 개가 목록에 안 보이고** 85개 정도만 보였습니다.

**원인**: `[pre0]` 로더 스캐폴딩은 배포 초반에 돌고, streamsets/spring 자동 변환 카탈로그는
그보다 **뒤인** `[4b/5]` 와 sync ops DAG 에서 생깁니다. 그래서 그 카탈로그들이 이번 배포에서
정적 로더를 못 받고 캐치올 파일(`zz_runtime_catalogs.py`) 하나로 몰렸고, 그 파일이
**DAG 247개를 지고 파싱에 30.817초** 걸렸는데 `core.dagbag_import_timeout` 이 **30.0초**라
파일이 통째로 타임아웃했습니다. 등록은 446개인데 **직렬화된 것은 79개뿐**이라 목록에 안 뜬 것입니다.
(타임아웃은 import 에러 목록에 남지 않아 `[4] import 에러 없음` 으로 보이는 함정이 있습니다.)

**조치**: 자동 변환이 끝난 뒤 로더 스캐폴딩을 **한 번 더** 돌리는 `[4c/5]` 단계를 넣었습니다.
캐치올은 정적 로더가 가져가지 않은 것만 맡으므로 자동으로 비워지고, 파싱 부하가 파일 단위로
쪼개져 단일 파일 타임아웃이 사라집니다. **두 번째 배포를 기다릴 필요가 없습니다.**
배포 로그에 추가된 로더 수가 `(before → after)` 로 찍힙니다.

확인:
```bash
./scripts/deploy.sh dev      # [4c/5] 에서 "정적 로더 N개 추가" 출력 확인
./scripts/diagnose.sh        # [5] zz_runtime_catalogs 개수 급감 + [6] CHECKED 증가면 정상
```
그래도 남으면 컨테이너 env 로 `AIRFLOW__CORE__DAGBAG_IMPORT_TIMEOUT=120` 상향 후 `./deploy.sh up dev`.

> **참고 — 자동 변환이 "적용" 되려면**
> `diagnose.sh [12]` 에 `applied_ids=0 / mode=approval / staged_ids=472` 로 나오면 변환은 됐고 **적용만 대기** 중입니다.
> 최초 대량 반입은 무인 적용이 편합니다:
> ```bash
> podman exec --user airflow aa_airflow-webserver_1 \
>   airflow dags trigger cm_ops_sync_streamsets_autogen -c '{"mode":"auto"}'
> ```
> 이후 crontab 변경분 현행화는 원래대로 배치어드민 **자동 변환 검토** 화면에서 승인하시면 됩니다.

### 4-4. 22:16 판 이후 추가된 화면 수정 2건

| 항목 | 증상 | 조치 |
|---|---|---|
| 빌더 **공용 템플릿 미리보기** | 행에 올렸을 때 뜨는 미리보기 상자가 화면 오른쪽 밖으로 잘려 내용이 일부 안 보임 | 상자 폭을 테두리 포함으로 계산하고, 보이는 영역을 넘지 않도록 상한을 걸어 항상 화면 안에 들어오게 함 |
| 승인 화면 **좁은 폭(1024)** | 표 머리글 `처리 결과` 가 잘려 보임 | 열 폭 배분을 1%p 옮겨 해소(승인·반려 버튼 노출은 그대로 유지) |


### 4-5. 개명 요청(#4)의 마지막 잔재 정리

승인 화면의 요청 이름이 내부적으로 `실행주기 요청 #93002` 로 조립되고 있어, 3·4번 개명이
그 한 곳에서만 반영되지 않은 상태였습니다. **`스케줄 변경 요청 #93002`** 으로 통일했습니다.
실앱 승인 화면에서 옛 표기(`실행주기 요청`)가 **0건**, 새 표기가 정상 노출됨을 실측 확인했습니다.

## 5. 검증 결과 (반입 전)

기준 화면은 **1920x1080 브라우저 최대화**입니다.

| 축 | 결과 |
|---|---|
| 실앱 6화면 육안 실측 (목록·승인·빌더·자동변환·운영매니페스트·가이드) | **6/6, 오류 배너·로드 실패 없음** — 위 2건 수정 전후 동일(회귀 없음) |
| 브라우저 게이트(정본 스위트) | **67 PASS / 2 FAIL / 1 HOLD** |
| Java 통합 테스트 (maven 축) | 전건 통과 (오류 0) |
| Python 회귀 (pytest 축) | 전건 통과 |
| 번들↔샌드박스 패리티 | 일치(환경 전용 2파일 제외) |

## 6. 알려진 열린 항목 (이번 판에 포함되지 않음)

이번 변경으로 생긴 것이 아니며, 직전 판에서도 열려 있던 항목입니다. 숨기지 않고 적습니다.

| 항목 | 관측값 | 판단 |
|---|---|---|
| 화면 안내(투어) 말풍선이 대상 중심을 가림 (1920) | 대상 384~1536 · 말풍선 533~973 · 중심 (960,535) | 대상이 화면 폭 전체라 회피 여지가 좁음. 다음 판에서 배치 조정 |
| 사진 게이트 D27·D29 | D27 = 제품 실패 1건, D29 = 전제 미충족 | 자동 변환 도움말·다시 스캔 표면. 다음 판 |

`HOLD` 1건은 헤디드 전용 계약이라 정상입니다.
