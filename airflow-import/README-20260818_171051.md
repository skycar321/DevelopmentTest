# Airflow 제어 반입판 — 2026-08-18 (**화면 안내 결함 수정 1건**)

- 번들: `airflow-bundle-20260818_171051.7z` (1161441 bytes)
- sha256: `6f14089fa0d5da81db81fe72cda9f1f9a70618a2a16d116d229534a12059dc9f`
- 직전 판 `20260818_034535` 은 이 판에 모두 포함됩니다.

## ⚠ 먼저 읽어 주세요 — 이 판의 성격

**화면 안내(투어)의 알림 단계에서 안내가 가리키던 대상이 사라지던 문제를 고쳤습니다.**
그 외 화면·글자·배치·동작 변화는 없습니다. 바뀐 제품 파일은 두 개(알림 컴포넌트,
화면 안내 스크립트)이고, 각각 한 곳씩만 손댔습니다.

## 1. 무엇이 잘못돼 있었나

화면 안내가 알림 단계에 들어가면 알림 목록에 **예시 알림 한 줄**을 잠깐 넣고 그것을
가리킵니다. 그런데 바로 그때 실제 알림 목록을 받아오는 요청이 끝나면서 목록을
**통째로 다시 그렸고**, 그 과정에서 예시 줄과 안내가 가리키던 표시가 함께 지워졌습니다.

사용자 입장에서는 **안내 말풍선은 떠 있는데 가리키는 대상이 없는** 상태로 보입니다.

**알림이 하나도 없는 계정에서는 이 문제가 드러나지 않습니다.** 그래서 지금까지
발견되지 않았습니다. 알림이 쌓인 계정일수록 확실하게 재현됩니다.

## 2. 어떻게 고쳤나

화면 안내가 알림 목록을 쓰는 동안에는 **그 목록에 "안내가 사용 중" 표시를 남기고**,
알림 컴포넌트는 그 표시가 있으면 목록을 다시 그리지 않습니다.
안내가 끝나면 표시는 자동으로 지워지고 목록은 평소대로 갱신됩니다.

- 알림 개수 배지와 머리글은 **안내 중에도 계속 갱신**됩니다.
- 안내를 켜지 않은 평소 사용에는 **아무 변화가 없습니다.**

## 3. 확인한 내용

실제 앱에서 직접 재현하고 고친 뒤 다시 측정했습니다.

| 항목 | 결과 |
|---|---|
| 고치기 전 | 안내의 알림 단계에서 대상이 사라짐 (실제 앱에서 재현) |
| 원인 관측 | 예시 줄이 **들어간 뒤 0.175초 만에** 실제 알림 19건으로 교체되는 것을 직접 확인 |
| 고친 뒤 | 같은 측정에서 문제 **0건** |
| 되돌려 확인 | 고친 부분 **한 줄만** 되돌리자 문제가 그대로 다시 나타남 (수정이 실제로 효과가 있음을 확인) |
| 통합 검사 | **76건 전부 통과**(실패 0) |

## 4. 반영 절차

직전 판과 동일합니다. 7z 전체 해제 → `batch-admin-reference/src/**` 등을 사내
`ui-batchadmin-main` 에 덮어쓰고 **jar 재빌드** → Airflow 서버는 `tar -zxvf airflow-bundle.tar.gz`
후 `./scripts/deploy.sh dev`. DB 반영(멱등)도 직전 판과 같습니다.

**이번 판은 화면 스크립트가 바뀌었으므로 재빌드가 필요합니다.**
반영 후 브라우저 새로고침 시 캐시가 남으면 강력 새로고침(Ctrl/Cmd+Shift+R) 한 번 부탁드립니다.

## 5. 확인 범위에 대한 정직한 한계

- 통합 검사 76건 중 실제 앱을 보는 것은 일부이고 나머지는 화면 조각 기반이라,
  **"전부 통과 = 실앱 전수 검증" 은 아닙니다.**
- 이번 수정 자체는 **실제 앱에서 재현·수정·되돌림 확인**까지 마쳤습니다.
