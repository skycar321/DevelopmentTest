# airflow-bundle 20260813_062258

배치어드민(Airflow 제어) 번들 — 샌드박스 실측 체제(1920x1080 단일 기준) 산출.

## 이번 반입분 주요 변경
- **UX 전수감사 High 전건 종결(실측)**: UX06(목록 설명 — DAG별 큐레이션 목적문 + 서버 operatorPurpose 필드 경로), UX10(투어 첫 스텝 밀도 — 목차 온디맨드), UX14(일괄 스케줄 모달 한글화·변경/생략 건수 예고), UX15(승인 대기/전체 이력 탭 패널 유지), UX16(스케줄 필수 안내+포커스, 필수 배지 3필드).
- UX13 렌더(최근 실행 range/total + 더보기), UX05(빈 실행중 목록 초기화 버튼), UX07(운영 Airflow 유틸 라벨), r11(승인대기 설명 원문 우선), r12(빌더 Task 편집기 비동기 재렌더 후 초점 복원).
- 투어: E108(목차의 표·승인 가림) 해소 1차 반영, 프리로드 게이트군 UX10 전제 정렬.
- 서버: 설명수정 불가 카피 서버·클라 일원화, operatorPurpose DTO/컨트롤러 경로(빌드 검증 통과).

## 검증 상태
- 스위트: **50 PASS / 3 FAIL / 1 HOLD** (~54게이트). 잔여 3은 전부 라우팅·진행 중: photo-scene(E108 후속 실측), compact-tour-click(목차 이동 계약 정렬), geometry-sweep(상세 개요 픽스처 정렬). 제품 신규 결함 아님 — 측정 계약 정렬 단계.
- 샌드박스 실제 앱(1920x1080 headed) 실측: 6화면 전항 특이사항 없음(레일겹침 0·표넘침 0·본문 가로스크롤 0·JS 오류 0), mvn 빌드 게이트 통과.

## 해제
```
7z x airflow-bundle-20260813_062258.7z   # 비밀번호 → airflow-bundle.tar.gz + docs/
tar -zxvf airflow-bundle.tar.gz
```
